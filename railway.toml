[build]
builder = "NIXPACKS"
buildCommand = "pip install playwright==1.44.0 && playwright install chromium && playwright install-deps chromium"

[deploy]
startCommand = "python bot.py"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
