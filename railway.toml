[build]
builder = "NIXPACKS"
buildCommand = "pip install playwright==1.44.0 && playwright install chromium && apt-get install -y libasound2t64 || true"

[deploy]
startCommand = "python bot.py"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
